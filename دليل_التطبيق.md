# دليل تطبيق نُزهَه - Nuzha

## نظرة عامة

**نُزهَه** هو تطبيق ويب تفاعلي متكامل لإدارة المحميات الطبيعية في المملكة العربية السعودية. يوفر التطبيق واجهة سهلة الاستخدام بتصميم صحراوي جميل يعكس الهوية السعودية.

## المميزات الرئيسية

### 1. نظام تسجيل الدخول
- واجهة تسجيل دخول أنيقة بتصميم صحراوي
- دعم تسجيل الدخول التقليدي
- دعم تسجيل الدخول عبر نفاذ (النظام الوطني للدخول الموحد)

### 2. لوحة التحكم الرئيسية
تحتوي على ستة أقسام رئيسية:
- **التصاريح**: إدارة تصاريح الزيارة والرعي
- **موقعي**: خريطة تفاعلية للمحميات
- **بلاغ بيئي**: نظام الإبلاغ عن المخالفات البيئية
- **رحلاتي**: إدارة الرحلات السياحية
- **التوعية السياحية**: محتوى تثقيفي
- **الإعدادات**: إعدادات الحساب والتطبيق

### 3. نظام التصاريح
- إصدار تصاريح جديدة للزيارة أو الرعي
- اختيار المحمية من قائمة المحميات المتاحة
- تحديد تاريخ ومدة الزيارة
- عرض جميع التصاريح الصادرة
- نظام حالات التصاريح (قيد الانتظار، موافق عليه، مرفوض)

### 4. الخريطة التفاعلية
- عرض موقع المستخدم الحالي
- عرض مواقع المحميات على الخريطة
- تصنيف المحميات حسب المناطق

### 5. نظام البلاغات البيئية
- إرسال بلاغات عن المخالفات البيئية
- إرفاق صور للبلاغات
- تحديد الموقع الجغرافي للبلاغ
- متابعة حالة البلاغات

## التقنيات المستخدمة

### Frontend
- **Next.js 16.0.3**: إطار عمل React للتطبيقات الحديثة
- **React 19.2.0**: مكتبة بناء واجهات المستخدم
- **TypeScript**: لغة البرمجة المستخدمة
- **Tailwind CSS 4.1.17**: إطار عمل CSS للتصميم
- **Radix UI**: مكتبة مكونات واجهة المستخدم
- **React Hook Form**: إدارة النماذج
- **Zod**: التحقق من صحة البيانات
- **Lucide React**: مكتبة الأيقونات
- **Recharts**: مكتبة الرسوم البيانية

### Backend (جاهز للتفعيل)
- **Prisma 6.19.0**: ORM لإدارة قاعدة البيانات
- **SQLite**: قاعدة البيانات (يمكن تغييرها إلى PostgreSQL أو MySQL)

### الأدوات والمكتبات الإضافية
- **date-fns**: معالجة التواريخ
- **sonner**: إشعارات Toast
- **cmdk**: واجهة الأوامر
- **vaul**: مكتبة Drawer
- **embla-carousel**: مكتبة Carousel

## بنية المشروع

```
nuzha/
├── app/                      # صفحات التطبيق (Next.js App Router)
│   ├── page.tsx             # صفحة تسجيل الدخول
│   ├── dashboard/           # لوحة التحكم
│   ├── permits/             # صفحات التصاريح
│   ├── map/                 # صفحة الخريطة
│   ├── reports/             # صفحة البلاغات
│   └── api/                 # API Routes
│       ├── permits/         # API التصاريح
│       ├── protected-areas/ # API المحميات
│       └── reports/         # API البلاغات
├── components/              # مكونات React القابلة لإعادة الاستخدام
│   ├── ui/                  # مكونات واجهة المستخدم الأساسية
│   └── ...                  # مكونات مخصصة
├── lib/                     # مكتبات ووظائف مساعدة
│   ├── prisma.ts           # إعداد Prisma Client
│   └── utils.ts            # وظائف مساعدة
├── prisma/                  # إعدادات قاعدة البيانات
│   ├── schema.prisma       # نموذج البيانات
│   └── migrations/         # ملفات الترحيل
├── public/                  # الملفات الثابتة
├── styles/                  # ملفات CSS
└── hooks/                   # React Hooks مخصصة
```

## كيفية التشغيل

### المتطلبات
- Node.js 22.13.0 أو أحدث
- pnpm (مدير الحزم)

### خطوات التشغيل

1. **الانتقال إلى مجلد المشروع**
```bash
cd /home/ubuntu/nuzha
```

2. **تثبيت التبعيات** (إذا لم تكن مثبتة)
```bash
pnpm install
```

3. **تشغيل خادم التطوير**
```bash
pnpm dev
```

4. **فتح التطبيق في المتصفح**
```
https://3000-ivqe82ul2cs0pbo8w6303-301fedd2.manus-asia.computer
```

### أوامر إضافية

- **بناء التطبيق للإنتاج**
```bash
pnpm build
```

- **تشغيل التطبيق في وضع الإنتاج**
```bash
pnpm start
```

- **فحص الكود**
```bash
pnpm lint
```

## إعداد قاعدة البيانات (اختياري)

حالياً التطبيق يعمل بنظام تخزين مؤقت في الذاكرة. لتفعيل قاعدة البيانات الكاملة:

1. **تحديث ملف البيئة**
```bash
echo 'DATABASE_URL="file:./prisma/dev.db"' > .env.local
```

2. **توليد Prisma Client**
```bash
pnpm prisma generate
```

3. **تشغيل الترحيلات**
```bash
pnpm prisma migrate dev
```

4. **استعادة استخدام Prisma في API Routes**
   - افتح الملفات في `app/api/`
   - استبدل البيانات المؤقتة بـ Prisma queries

## نموذج البيانات

### User (المستخدم)
- `id`: معرف فريد
- `fullName`: الاسم الكامل
- `nationalId`: رقم الهوية الوطنية
- `phone`: رقم الجوال
- `email`: البريد الإلكتروني (اختياري)

### ProtectedArea (المحمية)
- `id`: معرف فريد
- `name`: اسم المحمية
- `region`: المنطقة
- `zoneType`: نوع المنطقة (GREEN, YELLOW, RED)
- `description`: وصف المحمية

### Permit (التصريح)
- `id`: معرف فريد
- `userId`: معرف المستخدم
- `protectedAreaId`: معرف المحمية
- `type`: نوع التصريح (VISIT, GRAZING)
- `visitDate`: تاريخ الزيارة
- `durationHours`: مدة الزيارة بالساعات
- `livestockCount`: عدد الماشية (للرعي)
- `status`: حالة التصريح (PENDING, APPROVED, REJECTED)
- `qrCode`: رمز QR للتصريح

### Report (البلاغ)
- `id`: معرف فريد
- `userId`: معرف المستخدم
- `protectedAreaId`: معرف المحمية (اختياري)
- `lat`: خط العرض
- `lng`: خط الطول
- `description`: وصف البلاغ
- `imageUrl`: رابط الصورة (اختياري)
- `status`: حالة البلاغ (NEW, INVESTIGATING, RESOLVED)

## المحميات المتاحة

1. **محمية الطبيق** - الرياض (منطقة خضراء)
2. **محمية الوعول** - الشرقية (منطقة صفراء)
3. **محمية أم الرمة** - القصيم (منطقة خضراء)
4. **محمية رجم الحمى** - الجوف (منطقة حمراء)
5. **محمية جزر فرسان** - جازان (منطقة صفراء)

### تصنيف المناطق
- **🟢 منطقة خضراء (GREEN)**: مفتوحة للزيارة بدون قيود
- **🟡 منطقة صفراء (YELLOW)**: تحتاج إلى تصريح مسبق
- **🔴 منطقة حمراء (RED)**: محظورة حالياً

## التخصيص والتطوير

### إضافة محمية جديدة
عدّل ملف `app/api/protected-areas/route.ts`:
```typescript
const mockProtectedAreas = [
  // ... المحميات الموجودة
  {
    id: "area-6",
    name: "محمية جديدة",
    region: "المنطقة",
    zoneType: "GREEN",
    description: "وصف المحمية",
    createdAt: new Date().toISOString()
  }
]
```

### تخصيص الألوان والتصميم
عدّل ملف `styles/globals.css` لتغيير الألوان والخطوط.

### إضافة صفحة جديدة
1. أنشئ مجلداً جديداً في `app/`
2. أضف ملف `page.tsx` داخل المجلد
3. أضف رابط الصفحة في لوحة التحكم

## الأمان والخصوصية

- التطبيق يستخدم HTTPS للاتصال الآمن
- البيانات الحساسة محمية
- نظام المصادقة جاهز للتكامل مع نفاذ
- التحقق من صحة البيانات باستخدام Zod

## الدعم والمساعدة

للحصول على المساعدة أو الإبلاغ عن مشاكل:
- راجع ملف `README.md` الأصلي
- تحقق من السجلات في Terminal
- استخدم أدوات المطورين في المتصفح (F12)

## الترخيص

هذا التطبيق تم تطويره لأغراض تعليمية وتجريبية.

## ملاحظات مهمة

1. **الخريطة**: تحتاج إلى مفتاح API من Google Maps لتفعيل الخريطة بشكل كامل
2. **نفاذ**: يحتاج إلى تسجيل التطبيق في منصة نفاذ للحصول على بيانات الاعتماد
3. **قاعدة البيانات**: حالياً معطلة لتجنب مشاكل التوافق، يمكن تفعيلها لاحقاً
4. **الإشعارات**: يمكن إضافة نظام إشعارات Push للتنبيهات

## الخطوات التالية المقترحة

1. ✅ تفعيل قاعدة البيانات بشكل كامل
2. ✅ إضافة نظام مصادقة حقيقي
3. ✅ تكامل مع Google Maps API
4. ✅ تكامل مع نفاذ
5. ✅ إضافة نظام الإشعارات
6. ✅ تطوير تطبيق موبايل مرافق
7. ✅ إضافة لوحة تحكم للمسؤولين
8. ✅ نظام تقارير وإحصائيات متقدم

---

**تم التطوير بواسطة**: Manus AI
**التاريخ**: نوفمبر 2025
**الإصدار**: 1.0.0
